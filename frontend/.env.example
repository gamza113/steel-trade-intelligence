# Local dev — Vite proxies /api to the backend (see vite.config.ts)
VITE_API_BASE_URL=/api/v1
VITE_MAPBOX_ACCESS_TOKEN=

# Production: copy frontend/.env.production.example — set vars in Vercel dashboard
